/*************************************************************************************************\
  Наименование:
  Контур/Модуль:
  Примечание:
  Вид работы  |Номер           |Дата    |Исполнитель              |Проект
  -----------------------------------------------------------------------------------------------
\*************************************************************************************************/
#ifndef __DownLoadFilesToFolderVih__
#define __DownLoadFilesToFolderVih__


#component "GP"

//------------------------------------------------------------------------------

ObjInterface oDownLoadFilesToFolder;
public:
#doc
   Скачать файлы во временную папку
   возвращает указатель на длинную строку с путями к скачанным файлам через ";"
Параметры:
    _wTable    : word;  // код таблицы
    _cRec      : comp=0 // выбранная запись
#end
Function DownloadFiles(_wTable    : word;  // код таблицы
                       _cRec      : comp   // выбранная запись
                      ) : tptr;


end;


VipInterface DownLoadFilesToFolder implements oDownLoadFilesToFolder Licensed(free);
#end //FILESREF_File_hRef
