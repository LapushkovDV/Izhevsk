#component "F_Veks"
#include AccredDoc.vih
  var iAccredDoc : AccredDoc;

#include F_Veks##ForESB.vih



Interface EM_ForESB 'Функции для вызова из ESB для модуля F_Veks' EscClose, Cyan;

create view as
select  *
from
 X$FIELDS
;

procedure CalcAccredDocSumByOp(_cDoc: comp);
{

  iAccredDoc.CalcAccredDocSumByOp(_cDoc);
}

procedure CalcAccredDocSum(_cDoc : comp);
{
  iAccredDoc.CalcAccredDocSum(_cDoc);
}
End.
